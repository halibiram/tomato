package com.tomatomediacenter.data.local.dao

import androidx.room.Dao
// import com.tomatomediacenter.data.local.model.ExtensionEntity // Assuming ExtensionEntity exists

/**
 * Data Access Object for Extension entities.
 */
@Dao
interface ExtensionDao {
    // Define Extension related database operations here
    // Example:
    // @Query("SELECT * FROM extensions")
    // fun getAllExtensions(): Flow<List<ExtensionEntity>>
}
